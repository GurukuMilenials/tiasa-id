import Link from 'next/link';
import { services,rupiah } from '@/config/services';
export const metadata={title:'Layanan'};
export default function Services(){return <main className="mx-auto max-w-6xl px-5 py-14"><h1 className="text-4xl font-bold">Layanan tiasa.id</h1><p className="mt-3 max-w-2xl text-slate-600">Pilih jenis pekerjaan untuk melihat cakupan dan simulasi biaya. Biaya akhir disepakati setelah rincian lokasi dan kebutuhan dikonfirmasi.</p><div className="mt-10 grid gap-5 md:grid-cols-2">{services.map(s=><article key={s.slug} className="rounded-xl border border-slate-200 bg-white p-7 shadow-sm"><s.icon size={32} className="text-brand"/><h2 className="mt-4 text-2xl font-semibold">{s.name}</h2><p className="mt-2 text-slate-600">{s.scope}</p><p className="mt-5 font-semibold text-brand">Estimasi mulai {rupiah(s.base+s.step)}</p><Link className="mt-5 inline-block rounded-lg bg-brand px-5 py-2.5 font-semibold text-white hover:bg-brand-hover" href={`/layanan/${s.slug}`}>Lihat rincian</Link></article>)}</div></main>}
